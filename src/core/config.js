const config = {
	idLength: 6,
	taskLength: 10,
	timeDelay: 2000,
	filters: ['all', 'active', 'completed'],
	tasks: ['Go to shop',
		'buy groceries',
		'finish project',
		'fix bugs',
		'generate id'],
};

export default config;
